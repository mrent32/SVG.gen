const { circle } = require('./Shapes')

describe('circle', () => {
    describe('render', () => {
        it('Should make a circle')
        const shape = new circle()
        let color = red
        expect(circle.render(circle, color)).toEqual(circle)
    })
})